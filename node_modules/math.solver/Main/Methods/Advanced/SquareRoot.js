function SquareRoot(number) {
    if(!number) return new Error("[Number] No Number Specified")
    if(isNaN(number)) return new Error("[Number] Number, No Letters Or Symbols")
    return Math.sqrt(number)
}

module.exports = SquareRoot